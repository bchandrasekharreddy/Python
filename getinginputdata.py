# geting input data

number = input("enter the value")

# input function takes the vaue from the promt


# raw_input reads string from the promt but in 3.1 raw_input is not working
# we can directly read the string by usig input function itself


variable = (input("Enter the String"))

